
https://habr.com/ru/post/278085/
https://developer.hashicorp.com/consul/docs/install/glossary


*скачивается бинарник exe, запускается файл с конфигурацией json
bat файл:
consul.exe agent -dev  -config-file -xxx.json
pause

в json указаны
datacenter "somename"
data_dir куда сохранит метаданные (лучше та же директория папочка work например)
log_level
server true
bind_addr свой ip
bootstrap true
log_file адрес куда логи сохранять (лучше та же директория папочка work например)
start_join []
ui true

оно будет работать как сервер и хранить в себе данные о сервисе


*Каждый инстанс может работать в двух режимах: агент и сервер. 
Разница в том, что агент является точкой распространения информации, 
а сервер — точкой регистрации. Т.е. агенты принимают запросы только на чтение, 
а сервер может выполнять изменения уже имеющейся информации (регистрация и удаление сервисов).

*Фактически мы, в любом случае, выполняем запрос к локальному адресу, разница лишь в том, 
что запрос на чтение будет обработан агентом на локальном хосте, а запрос 
на изменение данных будет переадресован лидеру, который сохранит и распространит данные по всему кластеру. 
Если наш локальный агент не является лидером в данный момент, 
то наш запрос на изменение будет полностью обработан локально и распространен по кластеру.